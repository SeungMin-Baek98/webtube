extends ../base.pug


block content
        if errorMessage 
            span=errorMessage 

        form(method="POST")
            input(name="oldPassword" type="password" placeholder="기존 비밀번호"  required)
            input(name="newPassword" type="password" placeholder="새로운 비밀번호" required)
            input(name="newPasswordRechecked" type="password" placeholder="새로운 비밀번호 재확인" required)
            input(type="submit" value="비밀번호 변경하기")

