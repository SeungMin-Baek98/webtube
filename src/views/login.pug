extends base.pug

block content
    if errorMessage 
        span=errorMessage

    form(method="POST")
        input(name="username" type="text" placeholder="Username" required)
        input(name="password" type="password" placeholder="Passowrd" required)
        input(type="submit", value="로그인")
        br
        a(href="/users/github/start") 깃허브 계정으로 로그인하기 &rarr;
        br
        a(href="/users/kakao/start") 카카오 계정으로 로그인하기 &rarr;
    div     
        span 계정이 없으십니까?
        div
            a(href="join") 회원가입 하러가기 &rarr;