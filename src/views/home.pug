extends base.pug
include mixins/videoMixin

block content
    nav
        ul 
            li 
                a(href="/videos/upload") Video Upload 
    each video in videos
        +videoMixin(video)     
    else 
        h1 영상 데이터가 없습니다😭😭