extends base.pug

block content       
    h4  Change Title of #{videoEdit.title}
    form(method="POST")
        input(name="title", placeholder="Video Title", value=videoEdit.title, required)
        input(value="save", type="submit")