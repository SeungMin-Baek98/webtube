extends base.pug

block content       
    h4  Change Title of #{videoId.title}
    form(method="POST")
        input(name="title", placeholder="Video Title", value=videoId.title, required)
        input(value="save", type="submit")